{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "clewd启动",
            "request": "launch",
            "command": "bash start.sh",
            "cwd": "${workspaceFolder}"
        }
    ]
}